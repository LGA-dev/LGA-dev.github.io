
import { extendTheme } from '@chakra-ui/react'

const customTheme = extendTheme({
    colors: {
      primary: {
        light: "#b794f4",
        base: "#7e57c2",
        dark: "#5d37a8"
      },
      isabelline: "#f3f2ef",
      black: "#111"
    }
})

export default customTheme
